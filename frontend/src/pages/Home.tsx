import { Center } from '@mantine/core';
import { ClassesTable } from '@/components/ClassesTable';

export const Home = () => {
    return (
        <Center>
            <ClassesTable />
        </Center>
    );
};
